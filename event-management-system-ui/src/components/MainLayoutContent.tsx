import React from "react";

const MainLayoutContent = ({className}: any) => {
  return (
    <div className={`${className} bg-white text-black w-full p-1 overflow-y-auto`}>
      
    </div>);
}

export default MainLayoutContent;